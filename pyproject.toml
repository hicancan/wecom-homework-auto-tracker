[project]
name = "wecom-homework-auto-tracker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "openpyxl>=3.1.5",
  "pandas>=2.3.0",
  "pytest>=9.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["local"]
addopts = "-q"
